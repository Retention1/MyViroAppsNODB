import 'package:flutter/material.dart';
class DummyStatistic extends StatelessWidget {
  const DummyStatistic({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('PENIS ADRIAN BESAR'),
      ),
    );
  }
}