import 'package:flutter/material.dart';

class WidgetRevisi extends StatelessWidget {
  const WidgetRevisi({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('PENIS ADRIAN BESAR'),
      ),
    );
  }
}