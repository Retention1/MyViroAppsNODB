import 'package:flutter/material.dart';

class WidgetMintaSaran extends StatelessWidget {
  const WidgetMintaSaran({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      height: MediaQuery.of(context).size.height * 1,
      child: Center(child: Text('PENIS DHIO KECIL')),
    );
  }
}
